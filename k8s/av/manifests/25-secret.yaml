# Do NOT commit real credentials. Use `kubectl create secret` or a secrets
# manager (Sealed Secrets, External Secrets, Vault) in real deployments.
apiVersion: v1
kind: Secret
metadata:
  name: scan-db-credentials
  namespace: av-scanning
type: Opaque
stringData:
  PGHOST: "192.168.221.135"
  PGPORT: "5432"
  PGDATABASE: "av_scanner"
  PGUSER: "av_scanner_user"
  PGPASSWORD: "CHANGE_ME_STRONG_PASSWORD"
